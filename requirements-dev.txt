# Add a package here when a gate in AGENTS.md § "Before reporting a change complete" needs it to
# pass. That is the whole membership rule: a user runs the plugin having installed none of this.
# Read by the maintainer and by `.github/workflows/checks.yml`, so the two cannot drift.
# What it cost to find that out: docs/skills/daily/decision-log.md § "Three ways the gates read
# clean on Windows and fail on Linux".

pytest
pyright

# `zoneinfo` ships no zone database on Windows; this is the same install README.md
# § Prerequisites asks of a user.
tzdata

# Imported inside `take_screenshots` in skills/daily/scripts/screenshot_capture.py, so the
# module loads without them and only pyright needs them present. `setup_screenshot_pipeline.ps1`
# installs the same two on a user's machine.
mss
Pillow
